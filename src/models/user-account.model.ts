export type ServerAccount = {
  id: string;
  email: string;
  created_at: string;
  updated_at: string;
  name: string;
  phoneNumber: string;
};

export type Account = {
  id: string;
  email: string;
  createdAt: string;
  updatedAt: string;
  name: string;
  phoneNumber: string;
};
